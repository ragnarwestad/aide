# 2-analyse

Stub. A real `4-status.md` links to the other three files of its spec
folder, and the fixtures here are real ones — so the links have to lead
somewhere. Nothing reads this file: `parse-status.test.ts` loads the
status fixtures by name.
