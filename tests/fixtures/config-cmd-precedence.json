{
  "_comment": [
    "Spec 345: where AIDE_INSTALL_CMD/installCmd and AIDE_TEST_CMD/testCmd are",
    "read from, as one table both implementations are checked against.",
    "`_aide-spec-lib.sh`'s `aide_resolve_override` (bash) and the dashboard's",
    "`resolveInstallCmd`/`resolveTestCmd` (TypeScript) resolve this",
    "independently — the pair is hand-kept, like `worktreeLinks`, so the",
    "agreement is asserted rather than assumed.",
    "",
    "Unlike worktree-links-precedence.json, `.aide/config` WINS here when both",
    "set a value — the reverse order, because an install/test command can",
    "legitimately differ per machine while a worktree link cannot.",
    "",
    "`value` is what both sides must resolve to. `source` is what both sides",
    "report — `.aide/config` or `project.yaml`. null means: the key is not",
    "written in that file at all."
  ],
  "cases": [
    {
      "name": "config-only",
      "config": "echo config",
      "manifest": null,
      "value": "echo config",
      "source": ".aide/config"
    },
    {
      "name": "manifest-only",
      "config": null,
      "manifest": "echo manifest",
      "value": "echo manifest",
      "source": "project.yaml"
    },
    {
      "name": "both-set-config-wins",
      "config": "echo config",
      "manifest": "echo manifest",
      "value": "echo config",
      "source": ".aide/config"
    },
    {
      "name": "neither",
      "config": null,
      "manifest": null,
      "value": null,
      "source": null
    }
  ]
}
